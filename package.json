{
  "name": "site-cliente",
  "version": "2.0.0",
  "private": true,
  "description": "Site público e painel de gestão para clínica odontológica.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node database/migrate.js",
    "test": "node --test",
    "build:assets": "node scripts/prepare-assets.js && tailwindcss -c tailwind.config.js -i public/assets/tailwind-input.css -o public/assets/site.css --minify",
    "build": "npm run build:assets && npm test",
    "check": "node --check server.js && node --check database/migrate.js && node --check lib/security.js && npm test"
  },
  "keywords": [
    "odontologia",
    "agendamento",
    "clinica"
  ],
  "license": "UNLICENSED",
  "type": "commonjs",
  "engines": {
    "node": ">=24 <25"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mysql2": "^3.18.0",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@babel/standalone": "^8.0.4",
    "@fortawesome/fontawesome-free": "6.7.2",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "tailwindcss": "3.4.17"
  }
}
